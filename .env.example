# Resend: https://resend.com — create an API key, then:
#   RESEND_API_KEY=re_...
#   CONTACT_TO_EMAIL=where@inbox.test   (inbox to receive form submissions)
#   CONTACT_FROM_EMAIL=onboarding@resend.dev  (Resend’s test sender; use your verified domain in production)
# Optional: full URL to logo image (defaults to canonical site + /images/shioleno_full.svg)
# CONTACT_LOGO_URL=
RESEND_API_KEY=
CONTACT_TO_EMAIL=sales@shiolenomillwork.com
CONTACT_FROM_EMAIL=
